// Version compatible de THREE.js r158 (UMD format)
// Chargement depuis le CDN mais en format UMD pour compatibilité
document.write(
  '<script src="https://unpkg.com/three@0.158.0/build/three.min.js"></script>'
);
console.log("THREE.js r158 chargé via CDN (format UMD)");
