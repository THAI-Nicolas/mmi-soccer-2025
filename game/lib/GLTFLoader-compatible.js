// GLTFLoader compatible avec THREE.js r158 (format UMD)
// Chargement depuis le CDN après THREE.js
document.write(
  '<script src="https://unpkg.com/three@0.158.0/examples/js/loaders/GLTFLoader.js"></script>'
);
console.log("GLTFLoader r158 chargé via CDN (format UMD)");
